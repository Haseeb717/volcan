<%= form_for(scala) do |f| %>
  <% if scala.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scala.errors.count, "error") %> prohibited this scala from being saved:</h2>

      <ul>
      <% scala.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :finca_id %>
    <%= f.text_field :finca_id %>
  </div>

  <div class="field">
    <%= f.label :harvested %>
    <%= f.datetime_select :harvested %>
  </div>

  <div class="field">
    <%= f.label :qqt %>
    <%= f.number_field :qqt %>
  </div>

  <div class="field">
    <%= f.label :paid %>
    <%= f.text_field :paid %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
